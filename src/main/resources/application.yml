server.port: 8084

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://postgres:5432/catalogdb
    username: postgres
    password: postgres
    driverClassName: org.postgresql.Driver